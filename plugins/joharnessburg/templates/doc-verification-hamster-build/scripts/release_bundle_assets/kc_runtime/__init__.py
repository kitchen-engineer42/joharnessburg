"""kc_runtime — minimal runtime helpers bundled with each verification release.

Modules:
  - confidence: composite scoring + corner-case proximity
  - dashboard:  HTML dashboard rendering

This package is self-contained: it ships inside a release bundle and has
no dependency on John or joharnessburg. The verifier runs without them.
"""
